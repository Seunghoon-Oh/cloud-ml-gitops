apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-ml-management.fullname" . }}
  labels:
    {{- include "cloud-ml-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloud-ml-management.selectorLabels" . | nindent 4 }}
